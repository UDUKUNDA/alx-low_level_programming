#include "main.h"
#include <string.h>
/**
 * _strlen_recursion - return the length of a string
 *@s: a str to pass
 * Return: a value
 */
int _strlen_recursion(char *s)
{
	int n;

	return (n = strlen(s));
}
