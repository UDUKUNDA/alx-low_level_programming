#include "main.h"
#include <ctype.h>
/**
 * _isalpha - checks if an char is alphabet
 * @c: argument to be checked
 *
 * Return: a value on Succes
 */

int _isalpha(int c)
{
	if (_isalpha(c))
	{
		int a = 1;
		return (a);
	}
	else
	{
		int b = 0;
		return (b);
	}
}
