#include <ctype.h>
/**
 * _isalpha - checks if an char is alphabet
 * @c: argument to be checked
 *
 * Return: a value on Succes
 */

int _isalpha(int c)
{
	if (_isalpha(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
