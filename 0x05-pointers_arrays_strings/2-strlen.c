#include "main.h"
#include <string.h>
/**
 * _strlen - to return the length of the string
 * @s:a poiinter to string
 *
 * Return: 0 upon success
 */

int _strlen(char *s)
{
	int len;

	len = strlen(s);
	return (len);
}
