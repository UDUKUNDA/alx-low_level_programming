#include "main.h"
#include <string.h>
#include <stdio.h>
/**
 * _puts - will print the string to screen
 * @str: a pointer to the string
 *
 * Return: nothing
 */
void _puts(char *str)
{
	puts(str);
}
